GdeMoe - ????????? ?????????? ? backend ??? ????? ?????, ??????? ? ????? ? AI-???????????.

??????:
- Mobile: Android, Kotlin, Jetpack Compose, MVVM, Retrofit, Room.
- Backend: FastAPI, PostgreSQL, Alembic, Docker Compose; AI ???????? YOLO/CLIP (???? fallback).

???????? ???????????:
- ??????? ?????????? (Quick Add) ? ???????? ?????????? (Batch Add).
- ?????? ???????: ??? ? ??????? ? ???? ? ???????.
- AI Review: ??????? ??????????? ? Accept/Reject/Link + ??????????? ????????.
- ??????? ???????? ? ?????? ? ???????? AI.

?????? ? ??????:
- Mobile: `cd mobile && ./gradlew assembleDebug` (APK: mobile/app/build/outputs/apk/debug/app-debug.apk).
- Backend (Docker): `cd backend/docker && docker compose up -d --build`, ????? `docker compose exec api alembic upgrade head`.
- API health: http://localhost:8000/api/v1/health (?????????: `/api/v1/health/full`).

AI-?????:
- ???????? ????? `/api/v1/media/upload`, ?????? `/api/v1/ai/analyze`, ?????? `/api/v1/ai/detections`, ???????? `/accept`, `/reject`, `/review_log`.

?????:
- Mobile unit-tests: `cd mobile && ./gradlew testDebugUnitTest`.
- Backend unit-tests: `cd backend && python -m pytest`.

????????????:
- ???? QA: `docs/qa_plan.md`, ????????? ?????????: `docs/system_plan.md`, DevOps: `docs/devops_automation.md`.
